export enum InterviewMode {
    online = 'Online',
    offline = 'Offline'
}

export enum InterviewSlotStatus {
    scheduled = "Scheduled",
    completed = "Completed",
    cancelled = "Cancelled",
    skipped = "Skipped"
}